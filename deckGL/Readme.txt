installation/start:
===================
in repo root folder, run "yarn" command in console
after that run "npm install" followed by "npm run"
demo locally available at: http://localhost:8080/





